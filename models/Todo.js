const db = require('./db');

class Todo {
  constructor(id, name, isCompleted) {
    this.id = id;
    this.name = name;
    this.isCompleted = isCompleted;
  }

  // CREATE
  // RETRIEVE
  // UPDATE
  // DELETE
}

module.exports = Todo;
